#!/usr/bin/env bash
# Loops 1 to 10, bad luck on 4th, good luck on 8th, Best School otherwise
i=1
while [ "$i" -le 10 ]
do
    if [ "$i" -eq 4 ]
    then
        echo "bad luck"
    elif [ "$i" -eq 8 ]
    then
        echo "good luck"
    else
        echo "Best School"
    fi
    i=$((i + 1))
done
